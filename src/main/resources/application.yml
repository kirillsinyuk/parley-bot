spring:
  application:
    name: parley-bot
  messages:
    basename: i18n/messages
  data:
    mongodb:
      database: stickers
      uri: ${MONGO_URI}
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

bot:
  token: ${BOT_TOKEN}
